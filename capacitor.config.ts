import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tsheet.app',
  appName: 'employee-tsheet',
  webDir: 'dist/employee-timesheet',
  bundledWebRuntime: false
};

export default config;
